Version Meaning: (e.g V1.2.3)
V = version
1.0.0 = Major release, everything should work
X.1.0 = bugs fixed, features added, should work but nothing major
X.X.1 = test code for debugging current errors, attempt #, unlikely to work if not 0
